# =====================================================
# RESUME CHAMELEON - ENVIRONMENT VARIABLES
# =====================================================


# AI Provider Configuration
# Options: 'groq' (recommended), 'gemini', 'openai'
AI_PROVIDER=groq

# Google Gemini API (optional, if using Gemini)
# Get key: https://aistudio.google.com/app/apikey
GEMINI_API_KEY=

# Supabase Configuration (Backend - Server Side)
# Get from: https://supabase.com/dashboard → Your Project → Settings → API
SUPABASE_URL=
SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# Supabase Configuration (Frontend - Public)
# These are safe to expose in browser (with RLS enabled)
NEXT_PUBLIC_SUPABASE_URL= 
NEXT_PUBLIC_SUPABASE_ANON_KEY=
# Groq API Configuration (Recommended - Fast & Free)
# Get key: https://console.groq.com/keys
GROQ_API_KEY=
# OpenAI API (Optional - Paid)
# Get key: https://platform.openai.com/api-keys
# OPENAI_API_KEY=your-openai-api-key-here

# Kestra Configuration (Optional - for workflow automation)
KESTRA_API_URL=http://localhost:8080
KESTRA_API_TOKEN=your-kestra-token-here

# =====================================================
# SETUP INSTRUCTIONS:
# =====================================================
#
# 1. Copy this file:
#    cp .env.local.example .env.local
#
# 2. Get SUPABASE KEYS:
#    - Go to: https://supabase.com/dashboard
#    - Select your project (or create new one)
#    - Settings → API
#    - Copy: Project URL, anon key, service_role key
#    - IMPORTANT: service_role key is SECRET - never expose!
#
# 3. Get GROQ API KEY (Free & Fast):
#    - Go to: https://console.groq.com
#    - Sign up / Login
#    - API Keys → Create API Key
#    - Copy the key
#
# 4. Get KESTRA TOKEN (Optional):
#    - Start Kestra: docker-compose up -d
#    - Open: http://localhost:8080
#    - Profile icon → Settings → API Tokens
#    - Create new token
#
# 5. Restart your dev server:
#    npm run dev
#
# =====================================================
# SECURITY NOTES:
# =====================================================
# - .env.local is in .gitignore (will NOT be committed)
# - service_role key bypasses RLS - NEVER expose to frontend
# - anon key is safe to use in browser (with RLS enabled)
# - Keep all API keys secret and rotate regularly
# =====================================================
